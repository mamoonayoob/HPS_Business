[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

# Next.js App Router — do NOT set a manual "Publish directory" in Netlify UI.
# This plugin handles routing, SSR, and static pages automatically.
[[plugins]]
  package = "@netlify/plugin-nextjs"
