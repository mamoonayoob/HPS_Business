/**
 * Route group for non-home pages. Each section/page is responsible
 * for full-width backgrounds with Container-aligned content (Figma pattern).
 */
export default function InnerPageLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
